%nav.navbar
  %div.navbar-brand

  %ul.navbar-nav
    %li
      %button#profile-button{ type: "button", class: "btn btn-circle" }
        = link_to 'Logout   ', destroy_user_session_path, method: :delete, class: 'dropdown-item', data: { confirm: 'Are you sure?', turbolinks: false }

    %li.dropdown
      %a{ href: "#", class: "dropdown-toggle", id: "dropdownMenuButton", role: "button", "data-toggle" => "dropdown", "aria-haspopup" => "true", "aria-expanded" => "false" }
        %i.fa.fa-cog
        Logout
      %ul.dropdown-menu{ "aria-labelledby" => "dropdownMenuButton" }
        %li
          = link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item', data: { confirm: 'Are you sure?' }
